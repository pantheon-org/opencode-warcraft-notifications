name: Deploy Documentation

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: write
  pages: write

concurrency:
  group: deploy-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-docs:
    name: Build and Deploy Documentation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version: 'latest'

      - name: Cache dependencies
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-docs-${{ hashFiles('**/docs/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-docs-

      - name: Install dependencies
        run: |
          cd docs
          bun install --frozen-lockfile

      - name: Build documentation
        run: |
          cd docs
          bun run build
          echo "✅ Documentation built successfully"

      - name: Deploy to docs branch
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e # v4.0.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: docs
          publish_dir: ./docs/dist
          force_orphan: false
          enable_jekyll: false
          commit_message: 'docs: Deploy documentation from ${{ github.sha }}'
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      - name: Ensure GitHub Pages source is set to docs
        env:
          GITHUB_API: https://api.github.com
        run: |
          echo "Ensuring GitHub Pages source is 'docs' at path '/'..."
          set -euo pipefail
          repo="${{ github.repository }}"
          # idempotent PUT to set Pages source; requires pages: write permission
          curl -sS -X PUT \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "$GITHUB_API/repos/$repo/pages" \
            -d '{"source":{"branch":"docs","path":"/"}}' \
          | sed -n '1,200p'
      - name: Summary
        run: |
          echo "🎉 Documentation Deployment Complete!"
          echo ""
          echo "📚 Docs Branch: docs"
          echo "🔗 Documentation: https://github.com/${{ github.repository }}/tree/docs"
          echo "📝 Commit: ${{ github.sha }}"
          echo ""
          echo "✅ Deployment triggered by docs changes"
