/**
 * Plugin configuration module (compatibility layer)
 *
 * This file re-exports from the new modular structure in src/config/
 * for backward compatibility with existing imports.
 *
 * @deprecated Import directly from 'config' or specific sub-modules instead
 */

export * from './config/index.js';
