name: Sync Package Version

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  pull-requests: write
  actions: write
  checks: write

jobs:
  sync-package-version:
    name: Sync package.json version with tag
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.WORKFLOW_PAT || secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Extract version from tag
        id: version
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          VERSION=${TAG#v}
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION from tag: $TAG"

      - name: Check if package.json needs update
        id: check
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${{ steps.version.outputs.version }}"
          
          echo "Current package.json version: $CURRENT_VERSION"
          echo "Tag version: $TAG_VERSION"
          
          if [ "$CURRENT_VERSION" = "$TAG_VERSION" ]; then
            echo "needs_update=false" >> $GITHUB_OUTPUT
            echo "✅ Package.json version already matches tag version"
          else
            echo "needs_update=true" >> $GITHUB_OUTPUT
            echo "📦 Package.json needs version update: $CURRENT_VERSION → $TAG_VERSION"
          fi

      - name: Configure Git
        if: steps.check.outputs.needs_update == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Create version sync branch
        if: steps.check.outputs.needs_update == 'true'
        id: branch
        run: |
          BRANCH_NAME="sync-version/${{ steps.version.outputs.tag }}"
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
          
          git checkout -b "$BRANCH_NAME"
          echo "Created branch: $BRANCH_NAME"

      - name: Update package.json version
        if: steps.check.outputs.needs_update == 'true'
        run: |
          NEW_VERSION="${{ steps.version.outputs.version }}"
          
          # Update package.json version using Node.js
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '$NEW_VERSION';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
            console.log('Updated package.json version to:', pkg.version);
          "

      - name: Commit and push version sync
        if: steps.check.outputs.needs_update == 'true'
        run: |
          TAG_VERSION="${{ steps.version.outputs.version }}"
          BRANCH_NAME="${{ steps.branch.outputs.branch_name }}"
          
          git add package.json
          git commit -m "chore: sync package.json version to $TAG_VERSION

          Automatically generated by tag ${{ steps.version.outputs.tag }}
          
          [skip ci]"
          
          git push origin "$BRANCH_NAME"
          echo "Pushed version sync to branch: $BRANCH_NAME"

      - name: Create auto-merge PR
        if: steps.check.outputs.needs_update == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const tagVersion = '${{ steps.version.outputs.version }}';
            const tag = '${{ steps.version.outputs.tag }}';
            const branchName = '${{ steps.branch.outputs.branch_name }}';
            
            const prTitle = 'chore: sync package.json version to ' + tagVersion;
            const prBody = `## Version Sync

            This PR automatically syncs the package.json version with the newly created Git tag.

            - **Tag:** ${tag}
            - **Version:** ${tagVersion}
            - **Trigger:** Automated by tag creation workflow

            ### Changes
            - Updates package.json version from previous version to ${tagVersion}

            ### Auto-Merge
            This PR will auto-merge once all required checks pass, as it's a simple version synchronization.

            ---
            *This PR was automatically created by the Sync Package Version workflow.*`;
            
            // Create the pull request
            const { data: pr } = await github.rest.pulls.create({
              owner,
              repo,
              title: prTitle,
              head: branchName,
              base: 'main',
              body: prBody,
              draft: false
            });

            console.log('Created PR #' + pr.number + ': ' + pr.html_url);

            // Try to enable auto-merge on the created PR
            try {
              await github.graphql(`
                mutation($pullRequestId: ID!) {
                  enablePullRequestAutoMerge(input: {
                    pullRequestId: $pullRequestId,
                    mergeMethod: SQUASH
                  }) {
                    pullRequest {
                      autoMergeRequest {
                        enabledAt
                      }
                    }
                  }
                }
              `, {
                pullRequestId: pr.node_id
              });
              
              console.log('✅ Auto-merge enabled for PR #' + pr.number);
              
              // Add explanatory comment
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: pr.number,
                body: `🤖 **Auto-merge enabled**

This version sync PR will be automatically merged once all required checks pass.

**What happens next:**
1. 🔄 PR validation workflow will run (lint, test, type-check, build)
2. ✅ If all checks pass → automatic squash merge
3. ❌ If checks fail → manual review required

*Auto-merge was enabled by the Sync Package Version workflow.*`
              });
              
            } catch (error) {
              console.log('⚠️ Could not enable auto-merge (this is normal if auto-merge is not available):', error.message);
              console.log('The auto-merge-version-sync.yml workflow will handle merging after checks pass.');
            }

            console.log('✅ PR created successfully! Auto-merge configured.');
            console.log('PR URL: ' + pr.html_url);

      - name: Skip if no update needed
        if: steps.check.outputs.needs_update == 'false'
        run: |
          echo "✅ Package.json version already matches tag version - no sync needed"
          echo "Current workflow completed successfully with no changes required."